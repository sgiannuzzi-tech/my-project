[tool.poetry]
name = "my-project"
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12"
pandas = "^3.0.3"
scikit-learn = "^1.8.0"
jupyter = "^1.1.1"
pyod = "^3.5.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
